# Third-party notices

Tap Counter v1.0.0 has no runtime third-party dependencies.

The app uses browser-native APIs only, including localStorage, Clipboard API, Web Share API when available, Vibration API when available, and Screen Wake Lock when available.
