name: Build standalone
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and verify
        shell: powershell
        run: .\scripts\check-repository.ps1
      - uses: actions/upload-artifact@v4
        with:
          name: tap-counter-dist
          path: dist/
