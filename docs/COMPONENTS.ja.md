# コンポーネント

`components/` はランタイム依存ではなく、再利用するためのソーススニペットです。最終成果物は単一HTMLのままです。

## 確認ダイアログ

Tap Counterでは、リセットや削除などの破壊的な操作に `window.confirm()` ではなくアプリ内確認ダイアログを使用します。

キーボードフォーカス、`Esc` でのキャンセル、背景タップでのキャンセル、フォーカス復元、スマートフォン向けボトムシート表示に対応します。
