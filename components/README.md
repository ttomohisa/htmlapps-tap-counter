# Reusable components

This repository keeps the template's source-snippet component model. Components are copied/adapted into `src/index.template.html`; they are not runtime imports.

- `confirm-dialog.html` — confirmation dialog API compatible with `AppConfirm.ask({ title, message, confirmLabel, cancelLabel, tone })`.
